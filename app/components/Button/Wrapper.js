// Wrapper for Button

import styled from 'styled-components'

const Wrapper = styled.div`
  text-align: center;

  &.left {
    text-align: left;
  }

  &.right {
    text-align: right;
  }
`

export default Wrapper
